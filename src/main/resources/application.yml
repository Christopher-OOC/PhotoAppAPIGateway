server:
  port: 8082
spring:
  application:
    name: ApiController
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8010/eureka
